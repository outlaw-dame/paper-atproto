import React from 'react';
interface GestureViewProps {
    children: React.ReactNode;
    onDismiss?: () => void;
}
/**
 * A physics-based, gesture-driven view that supports swipe-to-dismiss.
 * Inspired by the fluid navigation of Facebook Paper.
 */
export declare const GestureView: React.FC<GestureViewProps>;
export {};
//# sourceMappingURL=GestureView.d.ts.map