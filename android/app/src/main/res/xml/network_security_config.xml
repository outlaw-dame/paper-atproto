<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Glympse (social.glympse.app)

  Production: all cleartext traffic is blocked (HTTPS only).
  Debug: localhost cleartext is allowed for Capacitor live-reload dev server.

  See: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext for local dev server during debug builds only. -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
